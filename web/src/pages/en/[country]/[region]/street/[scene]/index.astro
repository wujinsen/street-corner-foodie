---
import { streetLegacyRedirectFromAstro } from "../../../../../../lib/street-page-redirect";
import { streetStaticPaths } from "../../../../../../lib/static-paths";

export function getStaticPaths() {
  return streetStaticPaths();
}

return Astro.redirect(streetLegacyRedirectFromAstro(Astro), 301);
---
