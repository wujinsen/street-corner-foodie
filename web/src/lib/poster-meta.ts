/**
 * @deprecated v0.2.1 — UI copy lives in docs `web_posters` frontmatter.
 * Re-exports for scripts that still import this path.
 */
export {
  applyMeta,
  lookupPosterMeta,
  metaKey,
  resolveWebPosters,
  type PosterMeta,
  type WebPosterMeta,
} from "./lookup-poster-meta";
