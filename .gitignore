# Web
web/node_modules/
web/dist/
web/.astro/
web/.env
web/.env.*
!web/.env.example

# Junctions → repo root asserts (never commit duplicate trees)
web/public/asserts
web/public/asserts/
web/public/scf-img/
web/public/scf-img/

# Cursor image output (copy to asserts/ via workflow)
assets/
.cursor/checkpoints/

# Local-only · 不入 GitHub（本机 junction / 设计稿；CI 需自备 asserts 或对象存储）
asserts/
design/

# OS / logs
.DS_Store
Thumbs.db
*.log

# Stray downloads at repo root
*_format=jpg*.jpg
HHttd*.jpg
